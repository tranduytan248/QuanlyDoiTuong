@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("ContractStatus_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Edit", "ContractStatus", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ContractStatus_OnProcessSuccess(data, 'EditContractStatus')"
    }, new { @class = "form-horizontal", id = "EditContractStatus" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ContractStatus")
        </div>
    }
}