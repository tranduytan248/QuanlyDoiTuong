@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("Dossier_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Html.BeginForm("Edit", "Dossier", new { area = "Major" }, FormMethod.Post, new { encType = "multipart/form-data", id = "EditDossier", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_DossierDetail")
        </div>
    }

    <script type="text/javascript">
        $('form#EditDossier').ajaxForm({
            success: function (response) {
                if (response.status != undefined) {
                    if ($("#ModalContent #modal_EditDossier #chkNotDismissModal").is(":checked")) {
                        if (response.status != undefined) {
                            eval(response.message);
                            _tableDossier.ajax.reload(null, false);
                            response.status = undefined;
                            $("#ModalContent #modal_EditDossier form")[0].reset();
                        }
                    } else {
                        $("#ModalContent #modal_EditDossier").modal("hide");
                        $("#ModalContent #modal_EditDossier").on('hidden.bs.modal',
                            function () {
                                if (response.status != undefined) {
                                    eval(response.message);
                                    _tableDossier.ajax.reload(null, false);
                                    response.status = undefined;
                                }
                            });
                    }
                } else {
                    $("#ModalContent #modal_EditDossier" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}