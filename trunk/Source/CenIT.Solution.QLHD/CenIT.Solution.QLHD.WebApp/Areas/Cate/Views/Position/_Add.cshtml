@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Position_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "Position", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Position_OnProcessSuccess(data, 'AddPosition')"
    }, new { @class = "form-horizontal", id = "AddPosition" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Position")
        </div>
    }
}

@section ContinueForm{

}