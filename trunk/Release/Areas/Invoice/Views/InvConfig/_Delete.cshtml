@using TSFramework.App.Processors
@model Core.Inv.Models.MajorInvConfigModel

@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Delete"), AppProcessor.Messagor.GetMessage("SysConfig_Title"));
    Layout = "~/Views/Shared/_FormConfirm.cshtml";
}

@section FormType{
    danger
}

@section FormBody{
    @using (Ajax.BeginForm("Delete", "InvConfig", new { area = "Invoice" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "AppConfig_OnProcessSuccess(data, 'DeleteConfig')"
    }, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ConfigId)
        @Html.HiddenFor(model => model.ConfigKey)
        @Html.HiddenFor(model => model.ConfigValue)
        <h5> @Html.Raw(ViewBag.ConfirmMessage)</h5>
    }
}