@using TSFramework.App.Processors
@model Cores.Major.Models.MajorContractPaymentModel
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("ContractPayment_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fas fa-user-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("EditPayment", "Contract", new { area = "Major", contractId = Model.ContractId }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Payment_OnProcessSuccess(data, 'EditPayment')"
    }, new { @class = "form-horizontal", id = "EditPayment" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Payment")
        </div>
    }
}

@section ContinueForm{

}