@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("ContractStatus_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "ContractStatus", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ContractStatus_OnProcessSuccess(data, 'AddContractStatus')"
    }, new { @class = "form-horizontal", id = "AddContractStatus" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ContractStatus")
        </div>
    }
}

@section ContinueForm{

}