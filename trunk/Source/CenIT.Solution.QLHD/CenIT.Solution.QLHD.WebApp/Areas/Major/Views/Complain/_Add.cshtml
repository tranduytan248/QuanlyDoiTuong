@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Complain_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Html.BeginForm("Add", "Complain", new { area = "Major" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddComplain", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Complain")
        </div>
    }

    <script type="text/javascript">
        $('form#AddComplain').ajaxForm({
            success: function (response) {
                if (response.status != undefined) {
                    if ($("#ModalContent #modal_AddComplain #chkNotDismissModal").is(":checked")) {
                        if (response.status != undefined) {
                            eval(response.message);
                            _tableComplain.ajax.reload(null, false);
                            response.status = undefined;
                            $("#ModalContent #modal_AddComplain form")[0].reset();
                        }
                    } else {
                        $("#ModalContent #modal_AddComplain").modal("hide");
                        $("#ModalContent #modal_AddComplain").on('hidden.bs.modal',
                            function () {
                                if (response.status != undefined) {
                                    eval(response.message);
                                    _tableComplain.ajax.reload(null, false);
                                    response.status = undefined;
                                }
                            });
                    }
                } else {
                    $("#ModalContent #modal_AddComplain" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}

@section ContinueForm{

}