@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Ward_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "Ward", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Ward_OnProcessSuccess(data, 'AddWard')"
    }, new { @class = "form-horizontal", id = "AddWard" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Ward")
        </div>
    }
}

@section ContinueForm{

}