@using TSFramework.App.Processors
@model Cores.Major.Models.MajorDossierModel

@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Delete"), AppProcessor.Messagor.GetMessage("Dossier_Title"));
    Layout = "~/Views/Shared/_FormConfirm.cshtml";
}

@section FormType{
    danger
}

@section FormBody{
    @using (Ajax.BeginForm("Delete", "Dossier", new { area = "Major" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Dossier_OnProcessSuccess(data, 'DeleteDossier')"
    }, new { @class = "form-horizontal", id = "DeleteDossier" }))
    {
        <div id="bodyForm">
	        @Html.Partial("_DeleteBody")
        </div>
    }
}